package src.components;

/*
 * Resource.java
 * Enum for the resources
 * It needs to have an enum type for each resource
 * The types are "brick", "lumber", "wool", "grain", "ore"
 */

public enum Resource {
    BRICK, LUMBER, WOOL, GRAIN, ORE
}
