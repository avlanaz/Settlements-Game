package src.components;
import java.util.HashMap;
import java.util.Map;
import java.util.Set;

public class Board {
    // Graph containing nodes and edges
    // Resource nodes, road nodes, settlement nodes, and port nodes

    private Set<BuildNode> nodes;
    public Board() {
        /* 
        Initialise node positions based on the required number
        Assign a dice number to each resource node
        Connect resource nodes to settlement nodes
        Connect road nodes to settlement nodes
        Connect port nodes to settlement nodes
        Place the robber on the desert node 
        */
    }
}
